export default class Flight {
    constructor(time, number, direction, company, plane, status) {
        this.time = time;
        this.number = number;
        this.direction = direction;
        this.company = company;
        this.plane = plane;
        this.status = status;
    }
}